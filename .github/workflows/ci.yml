name: DevShield CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
      - run: npm test

  self-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Run DevShield against this repository
        uses: ./
        with:
          fail-on: critical
          comment: false
          exclude-paths: 'test/**'

  v1-consumer-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Resolve and run the public v1 compatibility ref
        uses: mabrig1/mabrig-devshield-ai@v1
        with:
          fail-on: critical
          comment: false
          exclude-paths: 'test/**'
